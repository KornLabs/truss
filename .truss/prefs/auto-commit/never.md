---
key: auto-commit
value: never
---

never run a writing git command (`commit`, `push`, `merge`, `rebase`, `reset`, `checkout` of a different ref) — leave the working tree as it is, name the paths you changed in your report, and let the human commit
