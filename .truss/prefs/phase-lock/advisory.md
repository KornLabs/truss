---
key: phase-lock
value: advisory
---

if a requested action violates the phase forbidden list: STOP, state the conflict, ask before proceeding; never proceed silently. The forbidden list binds spawned subagents too, recursively — the canonical inheritance rule is AGENTS.md §5.
