# Inbox

> Human notes between sessions. The agent never reads this automatically — only on the explicit command "process inbox" (docs/protocols.md).

## Inbox

<!-- Drop raw thoughts, tasks, and links here. Mark unfinished items with `wip:`. -->

## Processed

<!-- The agent appends "> [YYYY-MM-DD] processed → destination" here per item. -->
