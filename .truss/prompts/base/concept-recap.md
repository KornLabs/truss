Close the concept phase. The output is a decision, not more analysis.

Check the dossier is decision-ready:

- `concept.md` has all five sections filled, `#Recommendation` included.
- Every load-bearing assumption is marked validated / partly / untested with
  evidence — no silent leaps.
- The recommendation is unambiguous: **pursue** (with the next smallest step) or
  **park** (with the trigger that would reopen it). A park is a successful outcome.

Then run the phase-exit procedure (AGENTS.md §4): `doctor --gate`, spawn the gate
advocate if enabled, and write the `HT-NNN` exit entry summarising the
recommendation and any open risks. Record the pursue/park decision as a D-entry.
Do not change `current:` yourself — the human decides.

Update `state/current.md` (focus, what changed, next) and write the session
handover before you stop.
