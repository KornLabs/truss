Wrap the operate session. Leave the running system and its record in a clean state.

Check before you stop:

- `state/current.md` reflects reality — focus, what changed, blockers, next (≤5).
- Any incident this session is recorded with its cause and resolution; follow-ups
  are captured as D-entries or HUMAN-TODOS, not left in your head.
- Production changes were reversible and, where they touched architecture or risk,
  backed by a D-entry.
- `doctor` is clean (or every remaining finding is triaged and noted).

If `auto-commit: suggest`, propose the commit message for the work. `operate` has no built-in end — there is no phase after it; a major new direction reopens planning via the human, not a self-declared phase change.
