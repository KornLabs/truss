You are entering the build phase. Implement the plan — don't redesign it. The plan guides, it isn't a cage, but deviations are recorded as a D-NNN before you write them, never silent.

Input: {{INPUT}}   (optional — the task, or the path to the plan to build against)

Work this phase — purpose, forbidden list and exit are in the AGENTS.md phase block:
- Pull the next task from state/current.md (or pm/); one clear outcome at a time, marked in-progress.
- Implement in small, reviewable steps; keep tests green between them.
- If reality forces a deviation, record a new D-NNN before writing the deviating code.
- Don't redesign the architecture mid-build — flag the need as an HT-NNN for the human.

Done: working changes, green tests, deviations recorded as D-NNN, state/current.md current.
